import { PasswordsEqualDirective } from './passwords-equal.directive';

describe('PasswordsEqualDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordsEqualDirective();
    expect(directive).toBeTruthy();
  });
});
